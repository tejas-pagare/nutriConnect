<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Organization Team</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        /* Custom Styles - Variant of Admin Theme */
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f0f9f0, #d8eed8);
            color: #333;
        }

        .welcome-section {
            padding: 80px 0;
            background: linear-gradient(rgba(255, 255, 255, 0.95), rgba(255, 255, 255, 0.95)), url('https://via.placeholder.com/1500');
            border: 5px solid #38b44a; /* Different green shade */
            border-radius: 20px;
            max-width: 95%;
            margin: 50px auto;
            animation: fadeIn 1.5s ease-in-out;
        }

        .org-img {
            transition: transform 0.3s ease-in-out;
            border-radius: 15px;
            transition: 0.3s;
        }
        .org-img:hover {
            transform: scale(1.02);
        }

        .btn-org-primary {
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
            color: #fff;
            font-weight: 600;
            padding: 10px 25px;
            background-color: #38b44a; /* Different green shade */
            border: none;
        }

        .btn-org-primary:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 10px rgba(56, 180, 74, 0.5);
            background-color: #2e9c3e;
        }

        .btn-org-secondary {
            border-radius: 30px;
            transition: all 0.3s ease-in-out;
            color: #fff;
            font-weight: 600;
            padding: 10px 25px;
            background-color: #5a8f5a; /* Different muted green */
            border: none;
        }

        .btn-org-secondary:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 10px rgba(90, 143, 90, 0.5);
            background-color: #4d7e4d;
        }

        h1.display-4 {
            font-weight: 700;
            color: #38b44a; /* Matching primary green */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
            animation: slideInLeft 1s ease-in-out;
        }

        p.lead {
            font-size: 1.25rem;
            color: #555;
            animation: slideInRight 1s ease-in-out;
        }

        /* Animations */
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes slideInLeft {
            from {
                opacity: 0;
                transform: translateX(-50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes slideInRight {
            from {
                opacity: 0;
                transform: translateX(50px);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        @media(max-width:992px){
            .org-img{
                margin-top: 50px;
            }
        }
    </style>
</head>
<body>

    <!-- Organization Team Section -->
    <section class="welcome-section org-section">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6">
                    <h1 class="display-4 fw-bold mb-4">Welcome, Organization Team</h1>
                    <p class="lead mb-4">
                        Ensure the credibility of registered dietitians by reviewing and verifying their credentials. Help
                         maintain a trusted and professional network within Nutri Connect.
                    </p>
                    <div class="d-flex gap-3">
                        <a href="/organization_dash" class="btn btn-org-primary btn-lg">Go to Dashboard</a>
                        <a href="/contact" class="btn btn-org-secondary btn-lg">Contact Us</a>
                    </div>
                </div>
                <div class="col-lg-6">
                    <img src="https://img.freepik.com/free-vector/online-job-interview_23-2148613123.jpg?t=st=1741697893~exp=1741701493~hmac=abbb653dca5846944d59ee4da6a8d379bc1a2aa997daf62026c4e050037ec3a4&w=1380" alt="Organization Team" class="img-fluid rounded org-img" />
                </div>
            </div>
        </div>
    </section>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>